package mtOptimizer;

// This class contains the constructor to create a Bus object.
public class Bus extends Transportation {
	public Bus(int unit, int id, int capacity) {
		setUnit(unit);
		setId(id);
		setCapacity(capacity);
	}
}