package mtOptimizer;

// This class contains the constructor to create a Streetcar object.
public class Streetcar extends Transportation {
	public Streetcar(int unit, int id, int capacity, String letterCapacity) {
		setUnit(unit);
		setId(id);
		setCapacity(capacity);
		setLetterCapacity(letterCapacity);
	}
}
