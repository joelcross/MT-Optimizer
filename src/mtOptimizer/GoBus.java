package mtOptimizer;

// This class contains the constructor to create a GoBus object.
public class GoBus extends Transportation {
	public GoBus(int unit, int id, int capacity) {
		setUnit(unit);
		setId(id);
		setCapacity(capacity);
	}
}
