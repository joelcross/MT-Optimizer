package mtOptimizer;

// This class contains the constructor to create a GoTrain object.
public class GoTrain extends Transportation {
	public GoTrain(int unit, int id, int capacity) {
		setUnit(unit);
		setId(id);
		setCapacity(capacity);
	}
}
